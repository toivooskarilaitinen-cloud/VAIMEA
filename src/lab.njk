---
layout: base.njk
title: LAB
permalink: /lab/index.html
---
<header class="page-header">
  <p class="eyebrow">VAIMEA LAB</p>
  <h1>LAB</h1>
  <p>Oppimassa näkemään.</p>
</header>

{% set tools = [
  {
    number: "001",
    title: "MONITORI",
    url: "https://toivooskarilaitinen-cloud.github.io/monitori/",
    description: "Internetin päivittäinen tila viiden sensorin kautta: huomio, tieto, tapahtumat, aktiivisuus ja keskustelu."
  }
] %}

<ol class="essay-list essay-list--full">
  {% for tool in tools %}
    <li>
      <a href="{{ tool.url }}" target="_blank" rel="noopener noreferrer">
        <span class="essay-date">{{ tool.number }}</span>
        <span class="essay-title">{{ tool.title }}</span>
        <span class="essay-arrow">→</span>
      </a>
      <p>{{ tool.description }}</p>
    </li>
  {% endfor %}
</ol>
